"""
To use this module, you need to set **api_id, api_hash, phone, username** for the TelegramClient
as global variables in your code

"""

print("""To use this module, you need to set **api_id, api_hash, phone, username** for the TelegramClient
as global variables in your code""")

from .scrape_forwards import *